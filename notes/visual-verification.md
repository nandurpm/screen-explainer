# Local visual verification

The localhost workspace at `http://127.0.0.1:4074` displayed a synthetic dashboard image explicitly marked as having no private content, local upload/project/export controls, zoom/pan controls, manual region forms, two existing annotations, local-text action, and the external-analysis consent gate.

Selecting the Navigation region opened a structured local explanation with normalized region coordinates and annotation notes. No external endpoint was configured or invoked; the UI clearly required an endpoint plus explicit acknowledgement before external analysis could be requested.
