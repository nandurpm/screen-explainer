export async function extractLocalText(image){if(typeof globalThis.TextDetector!=="function")return{ok:false,kind:"unsupported",message:"Local text extraction is not supported by this browser."};try{const detector=new globalThis.TextDetector();const results=await detector.detect(image);return{ok:true,kind:"local",items:results.map(item=>({text:item.rawValue,rect:item.boundingBox?{x:item.boundingBox.x,y:item.boundingBox.y,width:item.boundingBox.width,height:item.boundingBox.height}:null}))};}catch(error){return{ok:false,kind:"failed",message:`Local text extraction failed safely: ${error.message}`};}}
export async function runExternalAnalysis({endpoint,project,confirmed,fetchImpl=globalThis.fetch}){if(!confirmed)throw new Error("External analysis requires an explicit privacy confirmation.");if(!endpoint)throw new Error("Configure an external endpoint before requesting analysis.");if(typeof fetchImpl!=="function")throw new Error("No network adapter is available.");try{const response=await fetchImpl(endpoint,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({project:{title:project.title,image:project.image,annotations:project.annotations}})});if(!response.ok)throw new Error(`External adapter returned ${response.status}.`);return await response.json();}catch(error){throw new Error(`External analysis did not complete: ${error.message}`);}}
